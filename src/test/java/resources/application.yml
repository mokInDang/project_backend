spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/mokindangdb?character_set_server=utf8mb4&allowPublicKeyRetrieval=true
    password: qwerqwer

  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        auto-ddl: create
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
